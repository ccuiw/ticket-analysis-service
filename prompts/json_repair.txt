# json_repair — JSON 格式自动修复提示词

## 用途
当大模型输出的 JSON 格式有误（如多余逗号、未闭合引号、markdown fence 包裹等）时，尝试自动修复为合法 JSON。

## 输入变量
- `{raw_output}`：模型原始输出文本
- `{expected_schema}`：期望的 JSON Schema

## 状态
待实现。初始化阶段不调用真实大模型，无需 JSON 修复逻辑。

## 要求
- 最多尝试 3 次修复
- 修复失败则返回原始错误信息
- 不得改变数据结构或值
